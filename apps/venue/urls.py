from django.urls import path

app_name = "venue"

urlpatterns = [
    # path("config/", views.RestaurantConfigView.as_view(), name="restaurant-config"),
    # path("tables/", views.TableListView.as_view(), name="table-list"),
]
